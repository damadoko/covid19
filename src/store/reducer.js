const initialState = {
  currentPage: null,
  totalEffectedCountry: 123,
  top3Country: [],
  hotNews: [],
};

const reducer = (state = initialState, action) => {
  return state;
};

export default reducer;
